import { AutorListFilterPipe } from './autor-list-filter.pipe';

describe('AutorListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AutorListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
